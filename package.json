{
  "name": "liveshare",
  "private": true,
  "scripts": {
    "asm": "bun src/assembler.js",
    "fib:c2sheet": "poetry -C src/llvm-to-shasm run llvm-to-shasm src/llvm-to-shasm/tests/fixtures/fib.c --ll-out src/llvm-to-shasm/tests/generated/fib.ll --asm-out src/llvm-to-shasm/tests/generated/fib.asm && ${BUN_BIN:-bun} src/assembler.js src/llvm-to-shasm/tests/generated/fib.asm out.sheet"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
